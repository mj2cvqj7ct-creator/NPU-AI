# Native Windows ARM64 build deps for Snapdragon X NPU.
#
# `onnxruntime-directml` does NOT publish a native win_arm64 wheel on PyPI
# (see microsoft/onnxruntime#27123), so on Snapdragon X we install the QNN
# execution provider package which targets the Hexagon NPU directly through
# Qualcomm's QAIRT SDK.
PyQt6>=6.6.0
numpy>=1.26.0
scipy>=1.12.0
sounddevice>=0.4.6
onnx>=1.15.0
# QNN EP for Snapdragon NPU (replaces onnxruntime-directml on ARM64).
onnxruntime-qnn>=1.22.0
pyyaml>=6.0
psutil>=5.9.0
pyinstaller>=6.3.0
comtypes>=1.3.0
pycaw>=20240210
# Streaming detector — Windows.Media.Control bridge (Spotify/Apple/YT Music).
winsdk>=1.0.0b10
